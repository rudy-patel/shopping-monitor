[pytest]
markers =
    integration: requires live Supabase and/or migrations (use `pytest -m integration` to run only these; `make test-backend` excludes them)
    no_generic_registry: scraper registry tests that must run without the generic fallback registered
